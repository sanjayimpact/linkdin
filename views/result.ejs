<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Scraped LinkedIn Profiles</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f8f9fa;
    }
    .card {
      border-left: 5px solid #0a66c2;
    }
    .card-title {
      font-size: 1.25rem;
      font-weight: 600;
      color: #0a66c2;
    }
    .card-subtitle {
      font-size: 0.95rem;
    }
    .profile-link a {
      word-break: break-all;
    }
  </style>
</head>
<body class="p-4">
  <div class="container bg-white p-4 rounded shadow-sm">
    <h2 class="mb-4 text-primary">🔍 Scraped LinkedIn Profiles for "<%= target %>"</h2>

    <% profiles.forEach((profile, index) => { %>
      <div class="card mb-4 shadow-sm">
        <div class="card-body d-flex align-items-start gap-3">
          <!-- Optional avatar placeholder -->
          <div>
            <img src="https://ui-avatars.com/api/?name=<%= encodeURIComponent(profile.name) %>&background=0a66c2&color=fff&size=64" alt="avatar" class="rounded-circle" width="64" height="64">
          </div>
          <div>
            <h5 class="card-title"><%= profile.name %></h5>
            <h6 class="card-subtitle mb-2 text-muted"><%= profile.jobTitle || profile.headline %></h6>
            <p class="mb-1"><strong> 📍  Location:</strong> <%= profile.location %></p>
            <p class="mb-1"><strong> 🧠 Headline:</strong> <%= profile.headline %></p>
            <p class="mb-1"><strong> 🧠 Education:</strong> <%= profile.Education %></p>
            <p class="mb-1"><strong> 👥 Mutual Connections:</strong> <%= profile.mutualConnections %></p>
            <p class="mb-1 profile-link"><strong>🔗 Profile:</strong> <a href="<%= profile.profileUrl %>" target="_blank"><%= profile.profileUrl %></a></p>
          </div>
        </div>
      </div>
    <% }) %>

    <div class="text-end">
      <a href="/" class="btn btn-outline-secondary mt-3">⬅️ Back</a>
    </div>
  </div>
</body>
</html>
